spring:
  application:
    name: ead-authuser-service
  config:
    import: 'configServer'
  cloud:
    config:
      discovery:
        serviceId: ead-config-server
      username: configserver
      password: 'ead123cs'